// enum 클래스 정의
// 클래스 생성시 enum 클래스 선택

package exam04;

public enum Transportation {

    // 상수니까 대문자 권장
    // static final -> 정적 상수
    // 이 정적 상수들은 사실 Transportation(Enum 클래스명)을 자료형으로 가진 객체
    SUBWAY,
    BUS,
    TAXI

}