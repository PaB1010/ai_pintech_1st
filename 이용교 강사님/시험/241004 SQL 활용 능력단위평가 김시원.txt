문항 1.
SELECT * FROM DITC USER_CONTRAINS WHERE TABLE_NAME = 'DEPTNO'

문항 2. 끝
SELECT E.ENAME, D.DEPTNO FROM EMP E JOIN DEPT D ON E.DEPTNO = D.DEPTNO;

문항 3. 끝
ALTER TABLE MEMBER2 ADD MOBILE VARCHAR2(15) NOT NULL;

문항 4. 6번 애매
1) 트랜잭션
2) TCL
3) 세션
4) COMMIT
5) ROLLBACK
6) 읽기성의 중요성